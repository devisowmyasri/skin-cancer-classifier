{
  "name": "obsidian-derm-suite",
  "version": "4.0.1",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install": "cd frontend && npm install",
    "dev": "cd frontend && npm run dev"
  }
}
